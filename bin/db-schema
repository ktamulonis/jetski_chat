#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
DB_PATH="${ROOT_DIR}/test.db"
SCHEMA_PATH="${ROOT_DIR}/db/schema.sql"

mkdir -p "${ROOT_DIR}/db"
sqlite3 "${DB_PATH}" ".schema" > "${SCHEMA_PATH}"
echo "Wrote schema to ${SCHEMA_PATH}"
